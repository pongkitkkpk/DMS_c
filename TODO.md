# TODO

งานที่คุยออกแบบไว้แล้วบางส่วน แต่ยังไม่ได้ลงมือทำ — เจ้าของระบบขอพักไว้ก่อน
(2026-08-27)

## ประธานสภานักศึกษาต้องเซ็นรับรองทุกโครงการก่อนเบิกเงิน + ล็อกแก้ไขหลังอนุมัติวงเงิน

**ไม่ใช่การพอร์ตจากระบบเดิม** — เช็คโค้ดเก่าแล้วไม่มีกฎนี้ ("สภานักศึกษา" ในโค้ดเก่ามีแค่
เป็นตัวอย่างชื่อชมรมในเอกสาร Swagger ไม่มีขั้นตอนอนุมัติพิเศษ) เป็น requirement ใหม่จาก
เจ้าของระบบ ต้องบันทึกเป็น deliberate deviation ใน `docs/DECISIONS.md` ตอนลงมือทำจริงด้วย
(ตาม Q22)

### ตกลงแล้วกับเจ้าของระบบ

1. **ขอบเขต**: ประธานสภานักศึกษาเห็น/เซ็นรับรองเฉพาะโครงการของชมรมในวิทยาเขตเดียวกับตัวเอง
   เท่านั้น (เช่น สภา มจพ.กรุงเทพฯ เห็นเฉพาะโครงการฝั่งกรุงเทพฯ) ไม่ใช่ทั้งมหาวิทยาลัย
2. **จุดบล็อก**: ต้องเซ็นรับรองก่อนโครงการจะเข้าสถานะ `BUDGET_APPROVED`
   ("เงินโครงการอนุมัติ") — เป็น hard gate บล็อก transition
   `PROJECT_APPROVED -> BUDGET_APPROVED` จริง ๆ ไม่ใช่แค่บันทึกไว้เฉย ๆ แบบที่
   `endorseAsAdvisor` ทำกับอาจารย์ที่ปรึกษาตอนนี้ (ซึ่งไม่บล็อกอะไรเลย)
3. **ล็อกการแก้ไข**: หลังโครงการเข้า `BUDGET_APPROVED` แล้ว ห้ามแก้ไข "รายละเอียดโครงการ"
   อีก ยกเว้นแนบไฟล์ (attachment ยังทำได้ปกติ)

### จุดที่ยังไม่ได้ยืนยันรอบสุดท้าย — Claude เสนอไว้ รอ owner คอนเฟิร์ม

เฟส `DRAFT_REPORT` ("ร่างสรุปผลโครงการ", ลำดับ 5) อยู่**หลัง** `BUDGET_APPROVED`
(ลำดับ 4) และ SH ต้องกลับมาแก้ไข "รายละเอียดโครงการ" ในเฟสนั้นเพื่อกรอกผลจริง
(จำนวนผู้เข้าร่วมจริง, กิจกรรมที่ทำจริง ฯลฯ) ผ่าน endpoint เดียวกัน
(`PUT /projects/:id/sections/:section`) กับตอนกรอกแผนตอนแรก — ถ้าล็อกแบบไม่มีเงื่อนไข
ตั้งแต่ `BUDGET_APPROVED` เป็นต้นไป SH จะเขียนรายงานผลโครงการ (กนศ.06) ไม่ได้เลย

ข้อเสนอ: ล็อกเฉพาะตอนที่โครงการ**อยู่ในสถานะ `BUDGET_APPROVED` เท่านั้น** (ช่วงคั่นระหว่าง
เงินอนุมัติกับเริ่มร่างรายงาน) พอเข้า `DRAFT_REPORT` แล้ว SH กลับมาแก้ไขได้ตามสิทธิ์เดิม
(`STUDENT_EDIT_PHASES` มีอยู่แล้ว) — ยังไม่ได้ถามยืนยันว่าใช่แบบนี้ หรือจริง ๆ ต้องการล็อก
ยาวไปถึง `DRAFT_REPORT`/`REPORT_SUBMITTED` ด้วย (แล้วต้องแยกกลไกให้ SH กรอกผลจริงทาง
อื่นแทน)

### งานที่ต้องทำเมื่อกลับมาต่อ

1. เปิด scope การมองเห็นโครงการข้ามชมรมให้ SH ของชมรม "สภานักศึกษา" เฉพาะในวิทยาเขต
   ตัวเอง (`scope.js` — ปัจจุบัน SH/AD เห็นได้แค่ชมรมตัวเอง `club_id` ตรงกัน เทียบกับ
   STUACT ที่ใช้ jurisdiction ข้ามชมรมได้ในกลุ่มชมรมของตัวเอง ต้องออกแบบเงื่อนไขคล้ายกัน
   แต่อิงตาม `campus_id` แทน)
2. เพิ่ม endorsement ใหม่คล้าย `signatureService.endorseAsAdvisor` แต่สำหรับประธานสภา —
   ต้องมี `signer_role` ค่าใหม่ (ใช้ 'SH' ซ้ำกับลายเซ็นเจ้าของโครงการเองไม่ได้ เพราะเป็นคน
   ละคนกัน) เก็บเป็น event ใหม่คล้าย `ADVISOR_ENDORSED`
3. เพิ่มเงื่อนไขใน `phaseService`/`scope.js` บล็อก transition
   `PROJECT_APPROVED -> BUDGET_APPROVED` จนกว่าจะมี signature ของประธานสภา(วิทยาเขต
   เดียวกับโครงการ)อยู่แล้ว
4. เพิ่มเงื่อนไขใน `scope.assertCanEdit` ห้ามแก้ไขขณะ `phase_code === 'BUDGET_APPROVED'`
   (รอยืนยันขอบเขตตามข้างบนก่อน) — endpoint แนบไฟล์ (`attachments.js`) ต้องไม่ใช้
   `assertCanEdit` ตัวเดียวกันนี้ต่อไป (ต้องแยกเป็นเงื่อนไขของตัวเอง ไม่ผูกกับตัวใหม่)
5. อัปเดตหน้า ProjectPage (frontend) ให้มีปุ่มเซ็นรับรองสำหรับประธานสภา คล้ายปุ่ม
   `permissions.endorseAsAdvisor` ที่มีอยู่แล้ว
6. บันทึกเป็น deliberate deviation ใน `docs/DECISIONS.md`
