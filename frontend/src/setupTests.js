/**
 * Loaded automatically by `react-scripts test` before every suite.
 *
 * Only the DOM matchers. Nothing here mocks the app: each test states its own
 * doubles, so a test that passes because of a global stub does not exist.
 */
import '@testing-library/jest-dom';
